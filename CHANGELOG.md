# XyForgeCrafting 更新说明

## 1.0.4 - 2026-07-30

- `FORGE_PROBABILITY` 改为在单个物品Lore中汇总失败和全部非零结果的最终概率。
- 最终概率为0的结果不显示；零失败、单品质配置只显示类似“传说: 100%”。
- 概率结果数量不再触发展示溢出；只需要至少一个概率槽，多个概率槽显示相同汇总Lore。
- 默认GUI采用服主确认的新布局，并在 `config.yml` 顶部内置全部7种组件type的中文说明。
- 默认布局加入实际可见的 `FORGE_RESULT` 成品预览槽；每个组件现在必须显式填写 `type`，漏写时安全拒绝重载。
- 默认概率槽由7个缩减为1个，默认动画材质调整为稳定的1.12.2物品 `FIREBALL`。
- 锻造成功后可配置替换末尾分隔线，并追加锻造者、成功时间和新结束分隔线。
- 同步写入锻造者UUID、当时名字、成功时间戳和配方ID隐藏NBT；普通鉴定物品不受影响。
- 新增时区、时间格式、Lore模板和末尾分隔线替换配置；旧配置缺少该节点时使用安全默认值。
- 锻造记录写入异常会执行事务回滚，避免扣除材料却交付不完整成品。
- 新增概率汇总、零概率隐藏、分隔线替换、占位符、时区和默认配置回归测试。

## 1.0.3 - 2026-07-30

- 修复部分Paper 1.12.2环境中锻造GUI被识别为普通箱子，展示物品可以拿取且所有功能按钮无效的问题。
- GUI会话不再使用脆弱的Inventory对象引用相等判断，改为校验玩家UUID与随机会话UUID。
- 每次打开锻造台都会生成独立会话身份，旧界面的关闭事件不会误操作当前新界面。
- 顶层槽位点击同时设置Bukkit取消状态与 `Event.Result.DENY`，覆盖左键、右键、数字键、Shift、双击和丢弃等点击路径。
- 点击与拖拽处理后在下一服务端刻重新渲染并同步背包，避免客户端显示可拿取的幽灵物品。
- 即使活动会话异常丢失，带XyForgeCrafting持有者的界面也会先拒绝所有交互再安全关闭，不再退化为可操作的普通箱子。
- 概率占位物品的默认Lore改为“放入图纸后显示锻造概率”，避免“品质”关键词触发外部TIP并追加 `0.0`。
- 运行时兼容清理旧版概率占位Lore中的“品质”和独立 `0.0` 行，无需覆盖服主已有的自定义GUI配置。
- 新增会话身份回归测试，验证相同玩家的旧会话以及不同玩家都无法命中当前GUI。

## 1.0.2 - 2026-07-30

- 主指令由 `/xyff` 更名为 `/xyfc`，完整别名 `/xyforge` 与 `/xyforgecrafting` 保持不变。
- 新增 `/xyfc get <配方ID> [数量]`，玩家可直接为自己取得当前服务器签名的真实图纸。
- 新增 `xyforgecrafting.get` 独立权限，默认仅OP，避免普通玩家任意生成图纸。
- `get` 与管理员 `give` 共用配方检查、数量限制、签名生成和安全待领取逻辑。
- 命令补全现在会按执行者权限展示子命令，并支持补全配方ID与常用数量。
- 命令帮助、插件描述、README与AI使用记录同步更新至1.0.2。

## 1.0.1 - 2026-07-30

- 从零建立Java 8、Paper/Spigot 1.12.2专用的Gradle Wrapper项目。
- 新增字符布局Bukkit GUI和可配置组件显示。
- 新增唯一图纸槽、HMAC签名图纸与隐藏NBT身份验证。
- 新增XyItems成品、失败/六品质最终概率展示及单次结算。
- 新增MythicMobs、XyItems和原版材料完整ID显示与匹配。
- 新增灵魂仓库与背包合并统计，默认优先扣除灵魂仓库。
- 新增金币、材料、随机结果、成品交付组成的事务流程和异常回滚。
- 新增失败图纸销毁/退还、材料百分比退款、金币百分比退款和自定义提示。
- 新增原版GUI火焰跳动动画。
- 新增Shift、数字键、拖拽、双击、Q、关闭和掉线边界保护。
- 新增持久化待领取队列，背包满时不会丢失退回图纸或退款物品。
- 新增锻造主命令、权限、补全与XyCore安全重载注册。
- 新增完整README、更新说明、AI使用记录和1.12.2构建说明。
