# AI 使用记录

## 项目范围

XyForgeCrafting当前版本1.0.4由AI在服主逐项确认玩法、配置和概率模型后辅助设计与实现。插件是服务器核心锻造玩法，目标环境固定为Java 8与Paper 1.12.2。

## 服主确认的关键需求

- `/xyfc open` 打开原版Bukkit GUI，当前不使用DragonCore。
- GUI使用字符布局，每个字符组件可配置材质、名称和Lore。
- 只有一个图纸槽；非图纸物品拒绝并提示。
- 放入图纸后才显示材料、拥有量、金币、全部非零最终概率和成品。
- 材料来自灵魂仓库和主背包，默认先扣灵魂仓库。
- 成品由XyItems立即确定品质和随机属性。
- 最终失败/品质概率存放在XyItems成品，不在配方重复配置成功率。
- 锻造失败可配置图纸销毁或退还、材料退款、金币退款和消息。
- 锻造过程需要原版GUI动画。
- 项目必须使用Gradle Wrapper；首个完整版本为1.0.1，当前功能版本为1.0.4，并提供完整文档。

## AI辅助决策

- 图纸真伪使用XyCore隐藏NBT身份、schema和HMAC签名，不严格匹配可见名称/Lore。
- 签名密钥首次启动本地生成，配置不暴露version、validation和GUI显示来源等实现字段。
- 成功交付是固定核心流程，不配置 `DELIVER_RESULT` 动作，减少错误配方造成扣材料不发物品。
- 事务在抽取前完成全部校验和扣除；抽取不可因主动制造的扣款失败被反复刷新。
- XyItems返回的成功结果已经携带确定品质物品，XyForgeCrafting不做二次品质抽取。
- 灵魂仓库通过1.1.1原子API扣除并获取退款凭据；未安装时只读取背包。
- 顶层GUI使用独立InventoryHolder和服务端会话状态，不信任客户端槽位内容。
- 关闭、掉线、重载与背包满退回统一进入持久化待领取路径。

## FishForgeCrafting审查记录

用户提供的FishForgeCrafting声明覆盖1.7.10到现代版本，主构建依赖包含1.12.2，但源码还存在1.7.10 NMS、NBTAPI、ItemsAdder、Rhino、数据库和新版Paper等混合结构。

本项目只吸取通用设计经验：InventoryHolder隔离、关闭归还、权重结果、动画与命令占位符。没有直接复制其源码，也没有引入跨版本和无关依赖。XyForgeCrafting明确只支持1.12.2。

## 验证记录

- 已执行 `gradlew.bat clean build --no-daemon`，包含测试的最终联合构建成功。
- 已确认构建目标和插件描述版本均为1.0.4。
- 已确认默认GUI六行均为9字符，并包含唯一图纸槽和唯一开始槽。
- 已确认默认配方使用完整材料ID和XyItems成品ID。
- 测试确认默认GUI为54格、图纸槽和开始槽唯一、概率槽为1个，并确认默认配方可加载。
- 另使用 `javac` 将17个主源码文件直接对照实际 `XyCore-0.3.10.jar` 与 `XyItems-1.0.2.jar` 编译，API签名验证成功。
- 已核对最终JAR未打入Paper、XyCore或XyItems编译桩。

## 1.0.2命令更新记录

- 根据服主要求将主指令由旧缩写迁移为 `/xyfc`，插件仅注册新的主缩写，保留完整别名 `/xyforge` 与 `/xyforgecrafting`。
- 新增 `/xyfc get <配方ID> [数量]`，仅玩家可执行，直接为本人生成带本服HMAC签名的图纸。
- 新增默认仅OP的 `xyforgecrafting.get` 权限，避免玩家在未授权时绕过图纸获取玩法。
- `get` 复用管理员 `give` 的配方查找、1到64数量校验、签名生成、堆叠拆分和背包满安全队列流程。
- 检查确认XyItems 1.0.2已经具有 `/xyitem get <物品ID> [数量]`、`xyitems.get` 权限与对应补全，无需重复修改XyItems。

## 1.0.3 GUI安全修复记录

- 服主联机测试发现锻造界面表现为普通箱子：展示物品可拿取、功能按钮无响应，据此判断整个Inventory事件未命中活动会话，而非单一点击类型遗漏。
- AI复查定位到会话查找要求事件提供的Inventory对象与创建时对象使用Java引用相等；此条件在实际Paper 1.12.2包装对象行为下不可靠。
- 改为在 `ForgeHolder` 与服务端 `ForgeSession` 中保存同一个随机会话UUID，并与玩家UUID共同验证，既兼容不同Inventory包装对象，也能隔离同一玩家的旧界面关闭事件。
- 所有锻造顶层点击均同时设置取消状态与 `Event.Result.DENY`，处理后下一刻重新渲染并调用背包同步，防止客户端残留幽灵物品。
- 增加失败关闭原则：只要Inventory持有者属于XyForgeCrafting，即使会话查找异常，也先禁止交互并关闭界面，绝不再按普通箱子放行。
- 根据服主截图确认概率占位Lore中的“品质”会触发服务器现有TIP并附加 `0.0`；新默认文本改为“锻造概率”，并在运行时清理旧配置中的触发词和独立数值行。
- 新增回归测试，覆盖正确会话命中、同玩家旧会话拒绝和不同玩家拒绝。

## 1.0.4 概率与成品记录重构

- 根据服主确认，将原先一个结果占一个概率格的模式改为单个概率物品Lore汇总；一个概率槽即可容纳任意数量结果。
- 概率为0的结果会隐藏，因此XyItems单品质、零失败配置显示为“传说: 100%”，不会出现无意义的“失败几率: 0%”。
- 多个 `FORGE_PROBABILITY` 槽位保留兼容，每个槽位显示同一份完整概率Lore；概率数量不再导致拒绝锻造，材料槽溢出保护保持不变。
- 采用服主提供的默认GUI布局，并把全部type说明直接写入默认 `config.yml`，减少维护时遗漏组件含义的风险。
- 检查发现服主给出的布局尚未引用已配置的字符 `6`；在第五行补入一个 `FORGE_RESULT` 槽，并增加显式 `type` 校验，防止漏写时静默退化为背景槽。
- 锻造成功后，在XyItems已经确定品质和随机属性的最终物品上追加锻造者与成功时间；只在末行确实是纯分隔线时删除，避免误删正常描述。
- 同一时刻通过XyCore隐藏NBT记录玩家UUID、当时名字、毫秒时间戳和配方ID，为以后查询、名匠排行或升级校验保留可信数据。
- 新增 `forge-record` 全局配置；旧服务器配置缺少节点时也启用Asia/Shanghai、标准时间格式和服主确认的四行Lore模板。
- 记录写入发生异常时走既有100%事务回滚，避免材料、金币与产物状态不一致。
- 测试覆盖零概率隐藏、汇总顺序、末尾分隔线识别、占位符替换、正常末行保护、上海时区和默认GUI。

## 仍需服主联机验证

- 使用生产Paper 1.12.2、Vault经济、MythicMobs 4.11和实际Xy系列JAR完整启动。
- 测试图纸放入、Shift/数字键/拖拽/Q/关闭/掉线等操作。
- 分别测试仅背包、仅灵魂仓库和两者混合扣除。
- 测试成功六品质、正常失败和异常回滚。
- 确认服务器最终材质包、物品Lore与GUI排版效果。
